export default function SetupPage() {
  return (
    <div style={{ padding: 40 }}>
      <h1>🎉 GiggleBox Setup</h1>
      <p>Create a child and sync your toy.</p>
      <a href="/dashboard">Skip → Dashboard</a>
    </div>
  );
}
